@{
    ViewData["Title"] = "Home Page";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PayrollFix - Admin Login</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #eef2f7;
        }
        header {
            background-color: #2e86de;
            padding: 20px 0;
            text-align: center;
        }
        .header img {
            width: 150px;
        }
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }
        .login-box {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 400px;
            margin: 60px auto;
        }
        .login-box h2 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #2e86de;
        }

        .login-box input[type="text"],
        .login-box input[type="password"] {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        .login-box button {
            width: 100%;
            padding: 12px;
            background-color: #ff5a60;
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 16px;
            cursor: pointer;
            margin-top: 10px;
        }

        .login-box button:hover {
            background-color: #ff4248;
        }

        .login-box .signup {
            margin-top: 15px;
            color: #555;
        }

        .login-box .signup a {
            color: #2e86de;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="login-box">
            <h2> Login</h2>
            <form action="#" method="POST">
                <input type="text" name="username" placeholder="Username" required>
                <input type="password" name="password" placeholder="Password" required>
                <a asp-action="Privacy">Forgot password</a>
                <button type="submit">Login</button>
                <p class="signup">Don't have an Account? <a asp-action="Signup">Signup Here</a></p>
            </form>
        </div>
    </div>
</body>
</html>
